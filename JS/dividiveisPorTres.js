// usando loops e operações condicionais, identificar quais números 
//entre 0 e 10.000 são múltiplos de 3 (ou divisíveis por 3).

let i = 0

while (i < 10000) {
    if ((i % 3) === 0){
        console.log(`${i} é múltiplo de 3`)
    } else {
    }
    i=i++
}
